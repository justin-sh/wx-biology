<view class="container">
  <view class='fav-list'>
    <block wx:for="{{items}}" wx:for-item="i" wx:key="fav-i">
      <text class="fav-item">{{index + 1}}. {{i}}</text>
    </block>
  </view>
</view>